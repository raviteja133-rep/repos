- name: variables
  hosts: local
  connection: local
  vars_files:
    - project.yaml

  tasks:
    - name: printing the variables
      ansible.builtin.debug:
        msg: "Project is {{ PROJECT }} Environment is {{ ENVIRONMENT }}"  