- name: installing nginx
  hosts: web
  become: yes

  tasks:
    - name: started nginx
      ansible.builtin.dnf:
        name: nginx
        state: installed
        
    - name: start nginx
      ansible.builtin.service:
        name: nginx
        state: restarted
        enabled: yes    
