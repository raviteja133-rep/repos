- name: installing nginx
  hosts: web
  become: yes
  
  tasks:
    - name: started nginx
      ansible.builtin.dnf:
        name: nginx
        state: installed

    - name: start and enable nginx
      ansible.builtin.service:
          name: nginx
          enabled: yes
          state: restarted
