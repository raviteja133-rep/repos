- name: installing nginx
  hosts: dev
  become: yes
  tasks:
    - name: install nginx
      ansible.builtin.dnf:
        name: nginx
        state: installed

    - name: start and enable nginx
      ansible.builtin.service:
          name: nginx
          enabled: yes
          state: restarted
